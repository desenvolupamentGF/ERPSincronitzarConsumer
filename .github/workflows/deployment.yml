name: Deployment_on_main

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  build:
    name: Deployment
    runs-on: [self-hosted, onpremise, consumer]
    steps:
      - name: Run deployment script on on-premise Debian
        run: /home/glaw/repositories/erp-gf-sync/syncConsumer.sh
    
